/**app.wxss**/
.container {
  height: calc(100% - 120rpx);
  overflow: auto;
  box-sizing: border-box;
  padding-bottom: env(safe-area-inset-bottom);
} 

page {
  height: 100%;
  background: #ffffff;
}
